PATTERN SEQ(stock a, stock b, stock c, stock d)
WHERE  skip-till-next-match
HAVING separate(a,b)
HAVING overlaps(b,c)
HAVING during(d,c)
AND a.symbol = 2 OR a.price < b.price
AND c.volume < 150
WITHIN 500